% Do not change document class, margins, fonts, etc.
\documentclass[a4paper,oneside,bibliography=totoc]{scrartcl}

% some useful packages (add more as needed)
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{latexsym}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{algorithm} % you can modify the algorithm style to your liking
\usepackage{algorithmic}
\usepackage{csquotes}
\renewcommand{\algorithmiccomment}[1]{\hfill\textit{// #1}}
\usepackage[usenames,dvipsnames]{xcolor}
\usepackage[colorlinks,citecolor=Green]{hyperref} % you may change/remove the colors
\usepackage{lipsum} % you do not need this
\usepackage{environ}
\usepackage{tikz}
\usetikzlibrary{positioning,shapes.gates.logic.US,shapes.gates.logic.IEC,automata,arrows, backgrounds}
\usepackage{natbib}
\bibliographystyle{chicagoa}
\setcitestyle{authoryear,round,semicolon,aysep={},yysep={,}} \let\cite\citep

% example enviroments and commands (add more as needed)
\newtheorem{definition}{Definition} \newtheorem{proposition}{Proposition}

\newcommand\todo[1]{\textcolor{red}{\framebox{\footnotesize TODO}\expandafter\ifx\expandafter\relax\detokenize{#1}\relax\else\ #1\fi}}


% DO NOT MODIFY
\newlength\oldparskip
\NewEnviron{answer}[1][8cm]{%
  \sbox{0}{\parbox[t][#1][t]{0.99\linewidth}{%
      \setlength{\oldparskip}{\parskip}%
      \setlength{\parskip}{.5em}%
      \BODY%
      ~%
      \setlength{\parskip}{\oldparskip}%
    }}%

  \begin{tikzpicture}
    % inner sep restores the \fbox padding
    \node[inner sep=\fboxsep, outer sep=0pt] (A) {\usebox{0}};
    % border thickness like \fboxrule
    \draw[line width=\fboxrule] (A.south west) rectangle (A.north east);
  \end{tikzpicture}%
  \vskip\baselineskip
}


\begin{document}

\title{DL26, Assignment 1}
\author{Anh Tu Duong Nguyen (anguyea, 2115931)\\
Anh-Nhat Nguyen (anhnnguy, 2034311)}
\date{\today}

\maketitle

\section*{Task 2a)}
\begin{answer}[12cm]
  \includegraphics[width=200pt]{python/a01_2_data.pdf}

  We conjecture how a fit with ReLU activation would look for different hidden-layer widths:
  \begin{itemize}\setlength{\itemsep}{0pt}
    \item[\textbf{(i)}] \textbf{0 hidden neurons:} The network reduces to linear regression. We expect a flat line through the data mean, unable to capture any non-linearity.
    \item[\textbf{(ii)}] \textbf{1 hidden neuron:} One ReLU unit introduces exactly one bend. We expect a ``V'' shape that captures at most one slope change.
    \item[\textbf{(iii)}] \textbf{2 hidden neurons:} Two ReLU units allow two bends. We expect an ``N'' shape that roughly outlines a single peak or valley.
    \item[\textbf{(iv)}] \textbf{3 hidden neurons:} Three ReLU units allow three bends. We expect a ``W'' shape that tracks the data's up--down--up pattern.
  \end{itemize}
\end{answer}

\section*{Task 2b)}
\begin{answer}[14cm]
  \noindent\begin{minipage}[t]{0.45\linewidth}
    \includegraphics[width=\linewidth]{python/a01_2_fit-2-neurons.pdf}
  \end{minipage}\hfill
  \begin{minipage}[t]{0.45\linewidth}
    \includegraphics[width=\linewidth]{python/a01_2_repeated-fits.pdf}
  \end{minipage}

  \vspace{0.3em}
  We train a 2-hidden-neuron FNN and obtain a training MSE of 0.0796 and a test MSE of 0.0867.

  \textbf{(i)} The result differs from our conjecture: instead of an ``N'' shape with sharp bends, the fit produces smooth, compressed step-like transitions. This occurs because the code uses sigmoid activation (not ReLU), which generates smooth S-curves rather than piecewise-linear bends.

  \textbf{(ii)} Repeating training produces inconsistent fits. Different runs converge to distinct local minima with costs of 0.080, 0.287, and 0.303. Random weight initialization places the optimizer at different starting points in a non-convex loss landscape. With only 2 neurons, the landscape contains multiple local minima, and BFGS converges to whichever basin it starts in.
\end{answer}

\section*{Task 2c)}
\begin{answer}[14cm]
  \noindent\begin{minipage}[c]{0.48\linewidth}
    \includegraphics[width=\linewidth]{python/a01_2_width-comparison.pdf}
  \end{minipage}\hfill
  \begin{minipage}[c]{0.48\linewidth}
    \centering
    \begin{tabular}{r r r}
      \toprule
      $h$ & Train MSE & Test MSE \\
      \midrule
      1   & 0.3729 & 0.3743 \\
      2   & 0.0796 & 0.0867 \\
      3   & 0.0073 & 0.0104 \\
      10  & 0.0059 & 0.0160 \\
      50  & 0.0030 & 2.0268 \\
      100 & 0.0021 & 6.3365 \\
      \bottomrule
    \end{tabular}
  \end{minipage}

  \vspace{0.3em}
  \textbf{(i)} As $h$ increases, the model transitions through three phases. \emph{Underfitting} ($h{=}1,2$): the network lacks capacity to capture the wave pattern. \emph{Optimal} ($h{=}3$): both MSEs reach low values (0.0073 and 0.0104), indicating the model captures the underlying function without memorizing noise. \emph{Overfitting} ($h{\geq}10$): training MSE continues to decrease, but test MSE diverges sharply (from 0.0160 at $h{=}10$ to 6.3365 at $h{=}100$) as the network memorizes noise.

  \textbf{(ii)} Our conjectures from (a) correctly predicted that $h{=}1$ and $h{=}2$ underfit and $h{=}3$ provides sufficient capacity. We did not anticipate the overfitting at $h{\geq}50$, where the model produces chaotic oscillations between training points.
\end{answer}

\section*{Task 2d)}
\begin{answer}[19cm]
  \begin{center}
  \begin{tabular}{@{}c@{\quad}c@{}}
    \includegraphics[width=110pt]{python/a01_2_distributed-reps.pdf} &
    \includegraphics[width=110pt]{python/a01_2_distributed-reps-scaled.pdf} \\[-0.3em]
    \includegraphics[width=110pt]{python/a01_2_distributed-reps-h3.pdf} &
    \includegraphics[width=110pt]{python/a01_2_distributed-reps-h3-scaled.pdf} \\[-0.3em]
    \includegraphics[width=110pt]{python/a01_2_distributed-reps-h10.pdf} &
    \includegraphics[width=110pt]{python/a01_2_distributed-reps-h10-scaled.pdf}
  \end{tabular}
  \end{center}
  \vspace{-0.8em}

  \textbf{(i)} The FNN obtains flexibility through linear combination of non-linear basis functions. Each hidden neuron produces a sigmoid curve; the output layer scales each by a learned weight and sums them. The scaled plots confirm this: the fit line equals the sum of the scaled hidden outputs.

  \textbf{(ii)} The $h{=}2$ representation is not intuitive. The unscaled plot shows two monotonic S-curves that reveal little about the fit. The scaled plot exposes extreme cancellation: weights of ${\approx}\pm547$ produce a peak of height~1.0 by subtracting two large opposing values. This cancellation strategy is mathematically opaque.

  \textbf{(iii)} The $h{=}3$ model is intuitive: output weights drop to $-15$ to $+40$, and each neuron maps to a distinct geometric feature (two valleys and one broad peak). The $h{=}10$ model abandons this interpretability: multiple neurons overlap or oppose each other, weights inflate to $-120$ to $+70$, and individual contributions become opaque.
\end{answer}

\section*{Task 2e)}
\begin{answer}[18cm]
  \textbf{Optional: Adam optimizer analysis.}

  We train an $h{=}50$ network using the Adam optimizer (learning rate 0.01, 5000 epochs) and compare against BFGS.

  Adam converges smoothly from an initial cost of 3.4, decreasing steadily over the first 2000 epochs before plateauing at approximately 0.35. It never reaches the BFGS optimum of 0.003 for the same architecture.

  This performance gap reflects the difference between first-order and second-order optimization. Adam uses only gradient information and maintains adaptive per-parameter learning rates. It converges stably but lacks curvature information, so it settles in broad, shallow basins. BFGS approximates the Hessian (second-order) and uses curvature to navigate narrow valleys in the loss landscape, reaching deeper minima in fewer iterations.

  For this low-dimensional problem (51 parameters for $h{=}50$), BFGS can afford to maintain its $O(n^2)$ Hessian approximation and achieves a superior solution. In high-dimensional settings (thousands of parameters), Adam's $O(n)$ memory cost and mini-batch compatibility make it the practical choice despite its weaker per-step convergence.
\end{answer}

\section*{Task 3a)}
\begin{answer}
  % YOUR ANSWER HERE
  There are 6 quantities computed using our parameters and inputs and targets in this forward pass.
  \begin{itemize}
    \item $z_{1} = W_{1}x$
    \item $z_{2} = W_{2} + b_{1}$
    \item $z_{3} = \sigma(z_{2})$ where $\sigma(x) = \frac{1}{1 + e^{-x}}$
    \item $z_{4} = W_{2}z_{3}$
    \item $\hat{y} = z_{4} + b_{2}$
    \item $l = (y- \hat{y})^{2}$
  \end{itemize}
\end{answer}

\section*{Task 3b)}
\begin{answer}[19cm]
  % YOUR ANSWER HERE
  There are in total 6 gradients to compute in this backward pass. The gradients are computed using backpropagation which is defined as follows:

  For a given operator $f: \mathbb{R}^I \to \mathbb{R}^O$, the gradient going outside of the operator back to the input is:
  \begin{center}
    $\delta_{in} = J_{f}^T \delta_{out}$ where $J_{f}$ is the Jacobian of $f$.
  \end{center}
  In the forward pass of the compute graph, the following computations are performed:
  \begin{itemize}
    \item matmul
    \item logistic activation is applied
    \item add 
  \end{itemize}
  Even though the compute graph figure shows that we have linear layers. The inputs to this linear layer are a matrix and a vector. According to the lecture, this equals
  matmul.
  The resulting gradient rules are for these operations:
  \begin{itemize}
    \item $\delta_{out}v^T$ or $A^T\delta_{out}$ for the matmul operation
    \item $\sigma(x)(1-\sigma(x))\delta_{out}$ for the logistic activation
    \item $\delta_{out}$ for all inputs in the add operation
  \end{itemize}
  Applying these rules of the compute graph, we get the following gradients:
  \begin{itemize}
    \item $\delta_{l} = 1$
    \item $\delta_{\hat{y}} = -2(y-\hat{y})\delta_{l}$
    \item $\delta_{y} = 2(y-\hat{y})\delta_{l}$
    \item $\delta_{z_{4}} = \delta_{\hat{y}}$
    \item $\delta_{b_{2}} = \delta_{\hat{y}}$
    \item $\delta_{W_{2}} = \delta_{z_{4}}z_{3}^T$
    \item $\delta_{z_{3}} = W_{2}^T\delta_{z_{4}}$
    \item $\delta_{z_{2}} = \sigma(z_{2})(1-\sigma(z_{2}))\delta_{z_{3}}$
    \item $\delta_{b_{1}} = \delta_{z_{2}}$
    \item $\delta_{W_{1}} = \delta_{z_{2}}x^T$
    \item $\delta_{x} = W_{1}^T\delta_{z_{1}}$
  \end{itemize}
\end{answer}

\newpage
\input{ai-declaration.tex}

\end{document}
